---
import Base from '../../layouts/Base.astro';
import ReviewCard from '../../components/ReviewCard.astro';
import CTABlock from '../../components/CTABlock.astro';
import { getCollection } from 'astro:content';

const allGuides = (await getCollection('guides', ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<Base
  title="Buyer's Guides - Field Service Stack"
  description="Curated software recommendations by trade. Find the best tools for HVAC, plumbing, electrical, landscaping, and general field service businesses."
>
  <section class="pt-12 pb-14 md:pt-16 md:pb-20">
    <div class="max-w-[65ch] mb-10">
      <h1 class="rise rise-1 mb-4">Buyer's Guides</h1>
      <p class="rise rise-3 text-muted text-base md:text-lg leading-relaxed font-body">
        Not every business needs the same software. These guides are organized by trade and company size so you can skip the noise and find what fits.
      </p>
    </div>

    {allGuides.length > 0 ? (
      <div class="reveal grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-3">
        {allGuides.map((post) => (
          <ReviewCard
            title={post.data.title}
            description={post.data.description}
            date={post.data.date}
            slug={post.id}
            collection="guides"
            category="Guide"
            rating={post.data.rating}
            image={post.data.image}
            imageAlt={post.data.imageAlt}
          />
        ))}
      </div>
    ) : (
      <p class="text-muted font-body">Guides coming soon.</p>
    )}
  </section>

  <!-- CTA -->
  <section class="pb-14 md:pb-20">
    <CTABlock />
  </section>
</Base>
